doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    if user
      button(class="btn", onclick="window.location='/chats'").
        MyChats
      button(class="btn", onclick="window.location='/user/"+user.id+"'").
        MyProfile
     else
       button(class="btn", onclick="window.location='/'").
         Login
    block content
